# Where the LMS build stands — Sept 8

Short version: the machine is set up and the first layer of the system is
running on it. Six things need an answer from you, and one of them matters far
more than the rest.

---

## What's working

The Mac Studio is configured and the data layer is live on it — the part that
takes a document, works out whose it is, files it in a consistent place, and
never files the same thing twice. It's tested (42 automated checks, all
passing on your machine, not just mine) and it rebuilds from source, so
nothing about it lives only in someone's head.

Storage is on the 12 TB array, which turned out to be already encrypted. Model
inference is running locally and is confirmed not reachable from the network —
no client or tax data leaves that machine.

## What I found and fixed

While setting up I checked every account on the Mac. There were **four**, not
the two anyone expected:

| Account | What it was |
|---|---|
| Your account | Yours. Fine. |
| `OpenClaw` | Created Aug 4, used for six minutes to run an installer, never again |
| `rescueadmin` | Created Aug 5 for a one-off task, deleted, folder left behind |
| `lms` | Created Aug 6, never used |

The `OpenClaw` one mattered. It was an **administrator**, it could **unlock the
encrypted disk at startup**, and its password was six sequential digits — the
kind you'd guess in a handful of tries. Anyone who had it had your whole
machine. It had been that way since Aug 4.

All three are gone, archived first in case anything was needed. Only your
account remains, and only your account can unlock the disk.

## One thing to change going forward

The setup document you sent had passwords in it for seven accounts — GitHub,
five email accounts, and Backblaze. It travelled through a few systems to reach
me, and I'd treat all seven as needing new passwords.

Nothing has gone wrong. But a password in a document is a password that exists
in every copy of that document, forever, and neither of us can now say how many
copies there are.

**Going forward:** change those seven, and send anything new through a password
manager share, or just type it in yourself while I'm on a screen share. It
takes the same amount of your time and leaves nothing behind.

---

## What I need from you

### 1. Your businesses and family — *this one actually blocks the build*

Everything else on this page is a sentence. This one is the system.

The part that decides "this letter belongs to C.H. Shink, not MRECAI" is built
directly from a list of names. Without the real list it can still file things,
but it will guess where a person's name is involved — and guessing wrong means
a client document in the wrong business's folder.

For each of the four businesses:

- **Legal name** exactly as it appears on the formation documents
- **EIN**
- **Domain**, if it has one

*(C.H. Shink is the one you flagged as incomplete — whatever you have is fine,
and if the answer is "that one's dormant," that's a useful answer too.)*

For the five people — you, Jesse, your mother, your father, and the household:

- **Full name, spelled exactly**
- **Every variant that shows up in real mail.** This is the part that gets
  skipped and shouldn't be. Mail doesn't arrive addressed the way the registry
  expects — "Matthew Epstein", "Matt Epstein", "M. Epstein", "Matthew R.
  Epstein" are four different strings to a computer and one person to you.

Handwritten on a napkin is fine. It's the content I need, not the format.

### 2. Google Workspace

Admin access, and four Shared Drives created: **MRECAI**, **C.H. Shink**,
**Atlase**, **MLECA**. This is where business documents get filed, so email
can't start without it.

### 3. Does C.H. Shink have an email address?

You sent five addresses and none of them is C.H. Shink's. If that entity
doesn't receive email — only paper — say so and I'll set it up as
document-only. I just don't want to guess and get it wrong.

### 4. Telegram

Turn on **two-step verification** on your Telegram account. That's it for now —
Telegram is how you'll approve things and how you'll stop the system if you
ever need to, so it gets locked down properly. I'll capture the rest
automatically the first time you message the bot.

### 5. Your FileVault recovery key

Printed, in a sealed envelope, somewhere you'd find it in an emergency but
nobody else would. If the disk ever needs recovering and that key is gone, the
data is gone — there's no back door, by design.

### 6. Who unlocks the Mac after a power cut?

The machine restarts by itself, but stops at a locked screen and **nothing runs
until a person types the password**. That's what encryption costs, and there's
no way around it that doesn't defeat the point.

I need a name, and a way that person finds out it happened. Planned restarts I
handle remotely; unplanned ones need someone in the room.

### 7. A repository under your account

Right now the code sits in a repository under *my* GitHub account so the build
could start. That's backwards — it should be yours, so that if I disappeared
tomorrow you'd still own everything and could hand it to anyone.

Create a free GitHub account (or use the one you set up) and I'll transfer it
across. Takes about a minute and it keeps the full history.

### 8. Which group chats, if any?

The system will read your texts to spot commitments and dates. **Individual
conversations only, by default — no group chats unless you name them.**

Groups contain other people's messages, and which of those you're comfortable
having read is your call to make, not mine. "None" is a perfectly good answer
and is what happens if you don't reply to this one.

---

## What happens next

Items 1, 2 and 3 unblock the email side, which is the part you'll notice first.
Items 4–6 are quick and are needed before the system goes fully locked down.

Once **item 1** lands I can finish the classification layer, and once **item 2**
lands mail starts flowing. Realistically those are the two that set the date.

Nothing here needs a meeting. Reply on this page, or send a voice note — a
photo of a handwritten list works fine for item 1.
